# Postgres Database URL (use either POSTGRES_URL or DATABASE_URL)
# For Vercel Postgres: Get from Vercel Dashboard -> Storage -> Postgres -> .env.local tab
# Use the POSTGRES_URL (pooled) connection string, NOT the non-pooling one
POSTGRES_URL="postgres://user:password@host:port/database?sslmode=require"

# Public base URL for the application
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
